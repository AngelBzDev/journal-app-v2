/* {
  notes: [],
  active: null || {
    id: id de la nota
    title: '',
    body: '',
    image: '',
    date: 475334574845
  }
} */

const init = {
  notes: [],
  active: null
}

const notesReducer = (state = init, action) => {
  switch (action.type) {

    default: return state
  }
}

export default notesReducer