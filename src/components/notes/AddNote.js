import React from 'react'
import Button from '../ui/Button'

const AddNote = () => {
  return (
    <div className=''>
      <Button color={'bg-primary'}>
        Agregar
      </Button>
    </div>
  )
}

export default AddNote